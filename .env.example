# ============================================================
#  Sao chép file này thành  .env  rồi điền giá trị thật.
#  KHÔNG commit file .env lên git.
# ============================================================

# --- Dịch thuật & bóc lời ---
# Chọn 1 trong 2 (theo translate.provider trong config.yaml):
# Claude: https://console.anthropic.com/
ANTHROPIC_API_KEY=
# Gemini: https://aistudio.google.com/apikey
# Nhiều key phân tách dấu phẩy -> tự xoay khi hết quota: key1,key2,key3
# Dùng cho CẢ dịch VÀ Gemini ASR (transcribe.provider=gemini).
GEMINI_API_KEY=

# --- Web / API ---
# Để TRỐNG = chế độ local mở (không cần key). Đặt giá trị khi deploy -> mọi
# request /api/* phải kèm header X-API-Key; n8n phải dùng cùng key này.
API_KEY=

# --- n8n (docker-compose) ---
N8N_USER=admin
N8N_PASS=doi-mat-khau-nay

# --- TikTok Content Posting API ---
# Đăng ký tại https://developers.tiktok.com/
TIKTOK_ACCESS_TOKEN=
TIKTOK_CLIENT_KEY=
TIKTOK_CLIENT_SECRET=

# --- Facebook Page (Graph API) ---
# Tạo app tại https://developers.facebook.com/ , lấy Page ID + Page Access Token
FB_PAGE_ID=
FB_PAGE_ACCESS_TOKEN=

# --- Instagram (Graph API, qua Facebook Business) ---
IG_USER_ID=
IG_ACCESS_TOKEN=

# --- (Tuỳ chọn) cookie để tải video bị giới hạn ---
# Đường dẫn tới file cookies.txt xuất từ trình duyệt
COOKIES_FILE=
